<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MHBank.Mobile.Views.HomePage"
             BackgroundColor="#F5F5F5">

    <Grid>
        <Label x:Name="LoadingLabel"
               Text="جاري التحميل..."
               FontSize="18"
               TextColor="Gray"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               IsVisible="true" />

        <ScrollView x:Name="ContentLayout" IsVisible="false">
            <VerticalStackLayout Spacing="0">

                <Grid BackgroundColor="White" Padding="20,50,20,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Label Text="☰" FontSize="24" TextColor="Black" VerticalOptions="Center" />
                    <Label Grid.Column="1" Text="MH Bank" FontSize="22" FontAttributes="Bold" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center" />
                    <Label Grid.Column="2" Text="🔔" FontSize="24" VerticalOptions="Center" />
                </Grid>

                <VerticalStackLayout Padding="20" Spacing="20">

                    <Label x:Name="WelcomeLabel" Text="مرحباً 👋" FontSize="26" FontAttributes="Bold" TextColor="Black" />

                    <Border BackgroundColor="#87CEEB" StrokeThickness="0" Padding="25" HeightRequest="180" Margin="0,10,0,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="20" />
                        </Border.StrokeShape>
                        <Grid RowDefinitions="Auto,*,Auto">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="💰" FontSize="20" VerticalOptions="Center" />
                                <Label Text="الرصيد الكلي" FontSize="16" TextColor="Black" VerticalOptions="Center" Opacity="0.9" />
                            </HorizontalStackLayout>
                            <Label x:Name="TotalBalanceLabel" Grid.Row="1" Text="0 IQD" FontSize="38" FontAttributes="Bold" TextColor="Black" VerticalOptions="Center" />
                        </Grid>
                    </Border>

                    <HorizontalStackLayout Spacing="10" Margin="0,10,0,5">
                        <Label Text="📊" FontSize="22" VerticalOptions="Center" />
                        <Label Text="حساباتي" FontSize="20" FontAttributes="Bold" TextColor="Black" VerticalOptions="Center" />
                    </HorizontalStackLayout>

                    <VerticalStackLayout x:Name="AccountsLayout" />

                    <HorizontalStackLayout Spacing="10" Margin="0,10,0,5">
                        <Label Text="📜" FontSize="22" VerticalOptions="Center" />
                        <Label Text="آخر المعاملات" FontSize="20" FontAttributes="Bold" TextColor="Black" VerticalOptions="Center" />
                    </HorizontalStackLayout>

                    <VerticalStackLayout x:Name="TransactionsLayout" Margin="0,0,0,20" />

                    <Button Text="تسجيل الخروج" BackgroundColor="#87CEEB" TextColor="Black" FontSize="16" FontAttributes="Bold" CornerRadius="12" Clicked="OnLogoutClicked" Margin="0,20,0,100" />

                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>